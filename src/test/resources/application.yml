server:
  port: 8081

spring:
  datasource:
    driver-class-name: oracle.jdbc.OracleDriver
    url: jdbc:oracle:thin:@ldap://oraoid1.portal.ss:389/ds32,cn=OracleContext,dc=portal,dc=ss
    username: MACA_ITINERARIO
    password: maca_itinerario

  jpa:
    database-platform: org.hibernate.dialect.Oracle12cDialect
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
kafka:
  bootstrap:
    servers: localhost
    
giss:

  application:
    name: mac1
    capp-code: MACA
    development-center: ced
    
  audit:
    enabled: false

  security:
    web:
      fake:
        enabled: true
        roles:
        authorities: READ_FAKE, WRITE_FAKE
        type-of-user: INTRANET

  kafka:
    default-config:
      secured-connection:
        enabled: false
  
  monitoring:
    #entorno
    environment: local